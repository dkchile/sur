// types/BackToTopButtonProps.ts
export interface BackToTopButtonProps {
    position?: "left" | "right"; // Posición horizontal del botón
    offset?: string; // Desplazamiento desde el borde, por ejemplo: "1rem", "20px"
}
